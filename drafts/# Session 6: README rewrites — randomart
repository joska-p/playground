Session 9: README rewrites — sequence-renderer + sequence-engine
Context
You are continuing a series of package README rewrites for the Creative
Playground monorepo. Each session rewrites one or two READMEs using the Atlas
Explorer voice defined in drafts/PLAN.md.
Read these files first:
- drafts/PLAN.md — Phase 2 template, tone rules, creative packages table
- packages/sequence-renderer/README.md — 89-line React + Canvas UI for rendering mathematical sequences
- packages/sequence-engine/README.md — 49-line pure-engine README for sequence generation
- packages/automa/README.md — reference for voice and two-package split handling (just completed)
- packages/automa-engine/README.md — reference for pure-engine README voice (just completed)
Scope
Rewrite two READMEs:
packages/sequence-renderer/README.md (89 lines) — React + Canvas UI layer. Renders mathematical sequences visually with a layer system, Fourier epicycle rendering, sidebar controls, viewport zoom/pan, and a Zustand store bridging engine logic to UI components.
packages/sequence-engine/README.md (49 lines) — Pure sequence generation engine. Two decoupled primitives: rules (pure functions defining sequences term-by-term) and an engine (generateSequence). Includes a PresetStore interface for persistence, a rule registry, and built-in rules like Recamán's sequence.
Key characteristics
- Two-package split: sequence-engine (pure logic, no DOM) and sequence-renderer (React + Canvas)
- sequence-engine provides createRule, registerRule, generateSequence, and a PresetStore interface with an InMemoryPresetStore
- sequence-renderer owns all browser-surface: canvas rendering, Fourier DFT computation (Web Worker), layer system, React components
- Zustand store with selector hooks (useSeed, useSequenceRule, useSequenceSteps, etc.) and mutation functions
- Component tree: App → ErrorBoundary → Sidebar → SequenceCanvas + SidebarControls
- The harmonicPath rule is registered at module level in App.tsx
- Layer system: configurable layer stack with per-layer param overrides
Template (from PLAN.md)
Same template as previous sessions — frontmatter, blockquote, Essence, Quick Launch, Field Notes, body sections
Instructions
1. Read all the files listed above
2. Rewrite both READMEs using the template and voice
3. For each file:
- Add frontmatter (title, coordinates, status "Active", date_discovered)
- Write evocative blockquote
- Write Essence (concept-first, why it exists)
- Write Quick Launch with install commands
- Write Field Notes (Catalyst, Quirks, Future Horizons)
- Restructure existing Architecture/Store/Component sections into Atlas Explorer body prose
- Preserve technical content (rule system, layer stack, Fourier computation, viewport, PresetStore) as reference material
- sequence-engine should emphasize the rule-as-function model and the PresetStore abstraction
- sequence-renderer should emphasize the canvas rendering strategy, layer system, and the engine↔UI bridge
4. Preserve the footer _Part of the Creative Playground (...) line in both files
5. Cross-reference between the two: sequence-renderer links to sequence-engine, sequence-engine can link back
Validation
pnpm sync-package-docs
Progress tracking
After completing, update drafts/PLAN.md checklist:
- Mark Session 9 as done
- Produce the new prompt for Session 10 (palette-generator + palette-engine READMEs)
