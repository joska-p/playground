# Session 7: README rewrites — art-canvas

## Context

You are continuing a series of package README rewrites for the Creative
Playground monorepo. Each session rewrites one or two READMEs using the Atlas
Explorer voice defined in `drafts/PLAN.md`.

**Read these files first:**
- `drafts/PLAN.md` — Phase 2 template, tone rules, creative packages table
- `packages/art-canvas/README.md` — 162-line WebGL shader-art generator README to rewrite
- `packages/mosaic-maker/README.md` — reference for voice and section handling
- `packages/pixel-manipulator/README.md` — reference for single-package session pacing

## Scope

Rewrite one README:

**`packages/art-canvas/README.md`** (162 lines) — Interactive WebGL canvas for
procedural shader art. The README is structured as a series of "Captain's log"
entries documenting the evolution of a shader assembly system: template layer,
preamble dependency resolution, module registry, mood/palette controls, and
decomposition of the generator into small functions.

## Key characteristics

- Shader assembly pipeline: template → module selection → dependency resolution → GLSL output
- Captain's log narrative style in the current README — these entries are valuable development history but need to be restructured into the Atlas Explorer format
- Module system: ShaderModules with category, deps, getCall, params
- Template system: structural skeletons with named slots (space, shape, effect, color)
- Preamble dependency graph for shared GLSL utilities
- Mood/palette as user-facing controls
- No framework dependency at the package level — pure WebGL + GLSL

## Template (from PLAN.md)

[Same template as previous sessions — frontmatter, blockquote, Essence, Quick Launch, Field Notes, body sections]

## Instructions

1. Read all the files listed above
2. Rewrite packages/art-canvas/README.md using the template and voice
3. For the file:
- Add frontmatter (title, coordinates, status "Active", date_discovered)
- Write evocative blockquote
- Write Essence (concept-first, why it exists)
- Write Quick Launch with install commands
- Write Field Notes (Catalyst, Quirks, Future Horizons)
- Restructure the Captain's log entries into coherent body sections in Atlas Explorer voice
- Preserve the technical content (module hierarchy, template system, dependency resolution, palette system) as reference material
- The Captain's log entries should inform the Field Notes and body prose, not remain as log entries
4. Preserve the footer _Part of the [Creative Playground](...) line

## Validation

pnpm sync-package-docs

## Progress tracking

After completing, update drafts/PLAN.md checklist:
- Mark Session 7 as done
- Produce the new prompt for Session 8 (automa + automa-engine README)
