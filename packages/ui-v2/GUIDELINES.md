# @repo/ui-v2 — Guidelines

> **This document lives at `src/App.tsx`** — the app is the single source of truth.
> Every component has an interactive demo with source code, API tables, and
> the full architectural docs (philosophy, setup, variants, conventions).
>
> Run `pnpm --filter @repo/ui-v2 dev` and open the page to see it.

### Quick links

| Topic                        | App.tsx section                                |
| ---------------------------- | ---------------------------------------------- |
| Philosophy                   | `#philosophy`                                  |
| Installation & setup         | `#setup`                                       |
| Architecture & file layout   | `#architecture`                                |
| Variant system & API tables  | `#variants`                                    |
| Component reference (demos)  | scroll below — each component has its own demo |
| Hooks reference              | `HooksSection`                                 |
| Conventions for contributing | `#contributing`                                |
