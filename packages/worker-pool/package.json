{
  "name": "@repo/worker-pool",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./src/worker-pool.ts",
    "./mock": "./src/mock-worker-pool.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test-watch": "vitest",
    "lint": "eslint .",
    "lint-fix": "eslint --fix .",
    "check-types": "tsc -b",
    "clean": "rm -rf node_modules dist .turbo"
  },
  "devDependencies": {
    "@repo/config-eslint": "workspace:*",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "eslint": "catalog:"
  }
}
