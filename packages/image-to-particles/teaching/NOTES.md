# Teaching Notes

- User wants fundamentals first, then build up to image-to-particles.
- Lessons are standalone HTML files — open directly in browser, no server.
- Keep each lesson short, one tangible win.
- User prefers working inside the package directory.
