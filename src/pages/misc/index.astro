---
import Layout from "@/layouts/layout.astro";

const baseUrl = import.meta.env.BASE_URL;
---

<Layout title="Misc">
  <article class="flex gap-8">
    <a href={`${baseUrl}misc/palettes-generator/`}>--&gt; Palettes generator</a>
    <a href={`${baseUrl}misc/piechart/`}>--&gt; Piechart</a>
    <a href={`${baseUrl}misc/examples/`}>--&gt; Examples</a>
  </article>
</Layout>
