/**
 * playground/apps/playground/src/lib/index.ts
 *
 * Barrel file for lib utilities — re-exports named utilities so callers
 * can import from '@/lib' or relative `../lib` paths.
 *
 * Follow the project's named-exports-only rule.
 */

export * from "./utils";
export * from "./math";
