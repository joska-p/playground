---
package: '@repo/randomart-engine'
kind: module
module: compile/compileToGLSL
title: compile/compileToGLSL
---

## Functions

### compileToGLSL()

> **compileToGLSL**(`treeR`, `treeG`, `treeB`, `behaviors`): `string`

Defined in: [compile/compileToGLSL.ts:81](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine/src/compile/compileToGLSL.ts#L81)

Compiles three expression trees (R, G, B channels) plus animation behaviors into a complete WebGL
2 fragment shader string, ready for `gl.compileShader()`.

#### Parameters

##### treeR

[`ExpressionNode`](types.mdx#expressionnode)

##### treeG

[`ExpressionNode`](types.mdx#expressionnode)

##### treeB

[`ExpressionNode`](types.mdx#expressionnode)

##### behaviors

[`AnimationBehavior`](types.mdx#animationbehavior)[]

#### Returns

`string`

#### Example

````ts
    const shader = compileToGLSL(treeR, treeG, treeB, [zoomBehavior]);
    ```;
````
