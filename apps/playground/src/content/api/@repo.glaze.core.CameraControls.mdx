---
package: '@repo/glaze'
kind: module
module: core/CameraControls
title: core/CameraControls
---

## Interfaces

### CameraControls

Defined in: [core/CameraControls.ts:8](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/glaze/src/core/CameraControls.ts#L8)

Explicit camera mutation contract. `Camera` stays passive; everything that changes its
`x`/`y`/`zoom` goes through here, so bounds and focal-point math are enforced in exactly one
place.

#### Methods

##### panBy()

> **panBy**(`dx`, `dy`): `void`

Defined in: [core/CameraControls.ts:10](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/glaze/src/core/CameraControls.ts#L10)

###### Parameters

###### dx

`number`

###### dy

`number`

###### Returns

`void`

##### panTo()

> **panTo**(`position`): `void`

Defined in: [core/CameraControls.ts:9](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/glaze/src/core/CameraControls.ts#L9)

###### Parameters

###### position

[`Point2D`](core.Camera.mdx#point2d)

###### Returns

`void`

##### reset()

> **reset**(): `void`

Defined in: [core/CameraControls.ts:14](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/glaze/src/core/CameraControls.ts#L14)

###### Returns

`void`

##### update()

> **update**(`partial`): `void`

Defined in: [core/CameraControls.ts:15](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/glaze/src/core/CameraControls.ts#L15)

###### Parameters

###### partial

`Partial`&lt;[`Camera`](core.Camera.mdx#camera)&gt;

###### Returns

`void`

##### zoomAt()

> **zoomAt**(`focalPoint`, `zoom`): `void`

Defined in: [core/CameraControls.ts:12](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/glaze/src/core/CameraControls.ts#L12)

###### Parameters

###### focalPoint

[`Point2D`](core.Camera.mdx#point2d)

###### zoom

`number`

###### Returns

`void`

##### zoomBy()

> **zoomBy**(`factor`, `focalPoint`): `void`

Defined in: [core/CameraControls.ts:13](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/glaze/src/core/CameraControls.ts#L13)

###### Parameters

###### factor

`number`

###### focalPoint

[`Point2D`](core.Camera.mdx#point2d)

###### Returns

`void`

##### zoomTo()

> **zoomTo**(`zoom`, `focalPoint?`): `void`

Defined in: [core/CameraControls.ts:11](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/glaze/src/core/CameraControls.ts#L11)

###### Parameters

###### zoom

`number`

###### focalPoint?

[`Point2D`](core.Camera.mdx#point2d)

###### Returns

`void`

## Functions

### createCameraControls()

> **createCameraControls**(`camera`, `minZoom?`, `maxZoom?`, `initial?`): [`CameraControls`](#cameracontrols)

Defined in: [core/CameraControls.ts:29](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/glaze/src/core/CameraControls.ts#L29)

Binds camera mutation to `camera` in place, clamping zoom to `[minZoom, maxZoom]` (both default
from `DEFAULT_ZOOM_BOUNDS`). `initial` is the state `reset()` restores; it defaults to a fresh
identity camera.

#### Parameters

##### camera

[`Camera`](core.Camera.mdx#camera)

The camera to mutate.

##### minZoom?

`number` = `DEFAULT_ZOOM_BOUNDS.minZoom`

Defaults to `DEFAULT_ZOOM_BOUNDS.minZoom`.

##### maxZoom?

`number` = `DEFAULT_ZOOM_BOUNDS.maxZoom`

Defaults to `DEFAULT_ZOOM_BOUNDS.maxZoom`.

##### initial?

[`Camera`](core.Camera.mdx#camera) = `...`

The state `reset()` restores; defaults to an identity camera.

#### Returns

[`CameraControls`](#cameracontrols)

The controls bound to `camera`.
