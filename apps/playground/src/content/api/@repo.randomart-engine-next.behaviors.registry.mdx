---
package: '@repo/randomart-engine-next'
kind: module
module: behaviors/registry
title: behaviors/registry
---

## Type Aliases

### ApplyCodeContext

> **ApplyCodeContext** = `object`

Defined in: [behaviors/registry.ts:46](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L46)

The GLSL variable names a behavior's `applyCode` emits code against.

#### Properties

##### color

> **color**: `string`

Defined in: [behaviors/registry.ts:50](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L50)

##### spatial

> **spatial**: `string`

Defined in: [behaviors/registry.ts:49](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L49)

##### speed

> **speed**: `string`

Defined in: [behaviors/registry.ts:48](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L48)

##### time

> **time**: `string`

Defined in: [behaviors/registry.ts:47](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L47)

---

### Behavior

> **Behavior** = `object`

Defined in: [behaviors/registry.ts:57](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L57)

A post-processing effect for the compiled shader: `kind` picks where its `applyCode` GLSL is
injected, and `glslFunction` adds shared helpers.

#### Properties

##### applyCode

> `readonly` **applyCode**: (`ctx`) => `string`

Defined in: [behaviors/registry.ts:62](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L62)

###### Parameters

###### ctx

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

##### glslFunction?

> `readonly` `optional` **glslFunction?**: `string`

Defined in: [behaviors/registry.ts:60](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L60)

##### id

> `readonly` **id**: `string`

Defined in: [behaviors/registry.ts:58](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L58)

##### kind

> `readonly` **kind**: [`BehaviorKind`](#behaviorkind-1)

Defined in: [behaviors/registry.ts:61](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L61)

##### label

> `readonly` **label**: `string`

Defined in: [behaviors/registry.ts:59](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L59)

##### noiseDependencies?

> `readonly` `optional` **noiseDependencies?**: readonly `string`[]

Defined in: [behaviors/registry.ts:63](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L63)

---

### BehaviorGroup

> **BehaviorGroup** = `object`

Defined in: [behaviors/registry.ts:124](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L124)

One category of behaviors (spatial or color) with its members, for pickers.

#### Properties

##### behaviors

> **behaviors**: `object`[]

Defined in: [behaviors/registry.ts:126](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L126)

###### id

> **id**: [`BehaviorId`](#behaviorid-1)

###### label

> **label**: `string`

##### label

> **label**: `string`

Defined in: [behaviors/registry.ts:125](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L125)

---

### BehaviorId

> **BehaviorId** = keyof _typeof_ [`BEHAVIORS`](#behaviors-1)

Defined in: [behaviors/registry.ts:109](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L109)

Union of the ids of every built-in behavior.

---

### BehaviorKind

> **BehaviorKind** = `"spatial"` \| `"color"`

Defined in: [behaviors/registry.ts:43](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L43)

What a behavior mutates: the coordinate `p` (spatial) or the output `color`.

## Variables

### BEHAVIORS

> `const` **BEHAVIORS**: `object`

Defined in: [behaviors/registry.ts:67](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L67)

The built-in behavior registry, keyed by behavior id.

#### Type Declaration

##### aurora-flow

> **aurora-flow**: `object` = `auroraFlowBehavior`

###### aurora-flow.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### aurora-flow.glslFunction

> `readonly` **glslFunction**: "vec3 auroraFlow(vec3 color, vec2 coords, float t, float speed) \{\n float wave1 = sin(coords.x \* 3.0 + t \* speed \* 0.4);\n float wave2 = sin(coords.y \* 2.0 - t \* speed \* 0.3 + wave1);\n vec3 aurora = vec3(\n 0.2 + 0.3 \* sin(wave2),\n 0.5 + 0.4 \* cos(wave1 \* 1.3),\n 0.6 + 0.3 \* sin(wave1 + wave2)\n );\n return mix(color, color + aurora \* 0.3, 0.5);\n\}\n"

###### aurora-flow.id

> `readonly` **id**: `"aurora-flow"` = `'aurora-flow'`

###### aurora-flow.kind

> `readonly` **kind**: `"color"` = `'color'`

###### aurora-flow.label

> `readonly` **label**: `"Aurora"` = `'Aurora'`

##### color-drift

> **color-drift**: `object` = `colorDriftBehavior`

###### color-drift.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### color-drift.id

> `readonly` **id**: `"color-drift"` = `'color-drift'`

###### color-drift.kind

> `readonly` **kind**: `"color"` = `'color'`

###### color-drift.label

> `readonly` **label**: `"Drift"` = `'Drift'`

###### color-drift.noiseDependencies

> `readonly` **noiseDependencies**: readonly \[`"smoothNoise"`\]

##### cosmic-maelstrom

> **cosmic-maelstrom**: `object` = `cosmicMaelstromBehavior`

###### cosmic-maelstrom.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### cosmic-maelstrom.glslFunction

> `readonly` **glslFunction**: "\nvec2 cosmicMaelstrom(vec2 coords, float t, float speed) \{\n float r = length(coords);\n float theta = atan(coords.y, coords.x);\n float logR = log(max(r, 0.04));\n float twist = 4.0 \* logR + sin(r \* 8.0 - t \* speed) \* 0.5;\n theta += twist \* 0.4;\n float pulse = 1.0 + 0.15 \* sin(t \* speed \* 0.8 - r \* 3.0);\n return vec2(cos(theta), sin(theta)) \* (r \* pulse);\n\}\n"

###### cosmic-maelstrom.id

> `readonly` **id**: `"cosmic-maelstrom"` = `'cosmic-maelstrom'`

###### cosmic-maelstrom.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### cosmic-maelstrom.label

> `readonly` **label**: `"Maelstrom"` = `'Maelstrom'`

##### crystal-facet

> **crystal-facet**: `object` = `crystalFacetBehavior`

###### crystal-facet.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### crystal-facet.glslFunction

> `readonly` **glslFunction**: "vec2 crystalHash(vec2 p) \{\n p = vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)));\n return fract(sin(p) \* 43758.5453123);\n\}\n\nvec2 crystalFacet(vec2 x, float t, float speed) \{\n vec2 scaled = x \* 3.0;\n vec2 n = floor(scaled);\n vec2 f = fract(scaled);\n float minDist = 8.0;\n vec2 cellOffset = vec2(0.0);\n for (int j = -1; j \<= 1; j++) \{\n for (int i = -1; i \<= 1; i++) \{\n vec2 g = vec2(float(i), float(j));\n vec2 h = crystalHash(n + g);\n vec2 cellCenter = g + h;\n float d = length(cellCenter - f);\n if (d \< minDist) \{\n minDist = d;\n cellOffset = h;\n \}\n \}\n \}\n float facetAngle = t \* speed \* 0.2 + cellOffset.x \* 6.2831;\n mat2 rot = mat2(cos(facetAngle), -sin(facetAngle), sin(facetAngle), cos(facetAngle));\n return rot \* x;\n\}\n"

###### crystal-facet.id

> `readonly` **id**: `"crystal-facet"` = `'crystal-facet'`

###### crystal-facet.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### crystal-facet.label

> `readonly` **label**: `"Crystal"` = `'Crystal'`

##### cyber-chroma-glitch

> **cyber-chroma-glitch**: `object` = `cyberChromaGlitchBehavior`

###### cyber-chroma-glitch.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### cyber-chroma-glitch.glslFunction

> `readonly` **glslFunction**: "\nvec2 cyberGlitch(vec2 coords, float t, float speed) \{\n float row = floor(coords.y \* 24.0);\n float timeline = floor(t \* speed \* 6.0);\n\n float trigger = step(0.92, fract(sin(row \* 41.134 + timeline) \* 43758.5453));\n float shift = (fract(sin(row \* 92.73 + timeline) \* 23412.18) - 0.5) \* 0.3 \* trigger;\n\n coords.x += shift;\n return coords;\n\}\n"

###### cyber-chroma-glitch.id

> `readonly` **id**: `"cyber-chroma-glitch"` = `'cyber-chroma-glitch'`

###### cyber-chroma-glitch.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### cyber-chroma-glitch.label

> `readonly` **label**: `"Matrix"` = `'Matrix'`

##### domain-warp

> **domain-warp**: `object` = `domainWarpBehavior`

###### domain-warp.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### domain-warp.glslFunction

> `readonly` **glslFunction**: "vec2 domainWarp(vec2 coords, float t, float speed) \{\n vec2 q = vec2(\n sin(coords.y \* 1.7 + t \* speed \* 0.6) \* 0.5 + sin(coords.x \* 2.3 + 1.2) \* 0.3,\n cos(coords.x \* 1.3 + t \* speed \* 0.4) \* 0.5 + cos(coords.y \* 1.9 + 0.7) \* 0.3\n );\n return coords + q \* 0.4;\n\}\n"

###### domain-warp.id

> `readonly` **id**: `"domain-warp"` = `'domain-warp'`

###### domain-warp.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### domain-warp.label

> `readonly` **label**: `"Warp"` = `'Warp'`

##### edge-detect

> **edge-detect**: `object` = `edgeDetectBehavior`

###### edge-detect.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### edge-detect.glslFunction

> `readonly` **glslFunction**: "vec3 applyLaplacianEdges(vec3 baseColor, vec2 uv, float time) \{\n float centerLuminance = dot(baseColor, vec3(0.299, 0.587, 0.114));\n\n float dX = dFdx(centerLuminance);\n float dY = dFdy(centerLuminance);\n float edge = length(vec2(dX, dY));\n\n float intensityModifier = 1.0 + smoothNoise(time \* 0.3) \* 1.0;\n edge \*= intensityModifier;\n\n vec3 edgeColor = 0.5 + 0.5 \* cos(time + uv.xyx + vec3(0.0, 2.0, 4.0));\n edgeColor \*= 1.5;\n\n return mix(baseColor, edgeColor, smoothstep(0.04, 0.2, edge));\n\}\n"

###### edge-detect.id

> `readonly` **id**: `"edge-detect"` = `'edge-detect'`

###### edge-detect.kind

> `readonly` **kind**: `"color"` = `'color'`

###### edge-detect.label

> `readonly` **label**: `"Contour"` = `'Contour'`

###### edge-detect.noiseDependencies

> `readonly` **noiseDependencies**: readonly \[`"smoothNoise"`\]

##### film-grain

> **film-grain**: `object` = `filmGrainBehavior`

###### film-grain.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### film-grain.id

> `readonly` **id**: `"film-grain"` = `'film-grain'`

###### film-grain.kind

> `readonly` **kind**: `"color"` = `'color'`

###### film-grain.label

> `readonly` **label**: `"Grain"` = `'Grain'`

###### film-grain.noiseDependencies

> `readonly` **noiseDependencies**: readonly \[`"random2d"`\]

##### glitch-blocks

> **glitch-blocks**: `object` = `glitchBlocksBehavior`

###### glitch-blocks.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### glitch-blocks.glslFunction

> `readonly` **glslFunction**: "vec2 glitchBlocks(vec2 coords, float t, float speed) \{\n float band = floor(coords.y \* 12.0);\n float step_t = floor(t \* speed \* 4.0);\n float trigger = step(0.85, fract(sin(band \* 12.9898 + step_t) \* 43758.5453));\n float seed = fract(sin(band \* 78.233 + step_t) \* 43758.5453);\n float shift = (seed - 0.5) \* 0.25 \* trigger;\n coords.x += shift;\n return coords;\n\}\n"

###### glitch-blocks.id

> `readonly` **id**: `"glitch-blocks"` = `'glitch-blocks'`

###### glitch-blocks.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### glitch-blocks.label

> `readonly` **label**: `"Glitch"` = `'Glitch'`

##### gravity-lens

> **gravity-lens**: `object` = `gravityLensBehavior`

###### gravity-lens.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### gravity-lens.glslFunction

> `readonly` **glslFunction**: "vec2 gravityLens(vec2 coords, float t, float speed) \{\n vec2 center = 0.6 \* vec2(cos(t \* speed \* 0.3), sin(t \* speed \* 0.37));\n vec2 delta = coords - center;\n float d = length(delta);\n float bend = 0.15 / (d \* d + 0.04);\n return coords - normalize(delta + vec2(0.0001)) \* bend;\n\}\n"

###### gravity-lens.id

> `readonly` **id**: `"gravity-lens"` = `'gravity-lens'`

###### gravity-lens.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### gravity-lens.label

> `readonly` **label**: `"Lens"` = `'Lens'`

##### gravity-well

> **gravity-well**: `object` = `gravityWellBehavior`

###### gravity-well.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### gravity-well.glslFunction

> `readonly` **glslFunction**: "\nvec2 gravityWell(vec2 coords, float t, float speed) \{\n vec2 p1 = 0.5 \* vec2(cos(t \* speed \* 0.4), sin(t \* speed \* 0.4));\n vec2 p2 = 0.5 \* vec2(cos(t \* speed \* 0.3 + 3.1415), sin(t \* speed \* 0.5));\n\n float d1 = length(coords - p1);\n float d2 = length(coords - p2);\n\n vec2 pull1 = (p1 - coords) \* (0.08 / (d1 \* d1 + 0.04));\n vec2 pull2 = (p2 - coords) \* (0.08 / (d2 \* d2 + 0.04));\n\n return coords + pull1 + pull2;\n\}\n"

###### gravity-well.id

> `readonly` **id**: `"gravity-well"` = `'gravity-well'`

###### gravity-well.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### gravity-well.label

> `readonly` **label**: `"Singularity"` = `'Singularity'`

##### hue-shift

> **hue-shift**: `object` = `hueShiftBehavior`

###### hue-shift.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### hue-shift.glslFunction

> `readonly` **glslFunction**: "vec3 hueRotate(vec3 color, float angle) \{\n float c = cos(angle);\n float s = sin(angle);\n return vec3(\n (0.299 + 0.701\*c + 0.168\*s) \* color.r + (0.587 - 0.587\*c + 0.330\*s) \* color.g + (0.114 - 0.114\*c - 0.331\*s) \* color.b,\n (0.299 - 0.299\*c - 0.328\*s) \* color.r + (0.587 + 0.413\*c + 0.035\*s) \* color.g + (0.114 - 0.114\*c + 0.292\*s) \* color.b,\n (0.299 - 0.299\*c + 1.250\*s) \* color.r + (0.587 - 0.587\*c - 1.050\*s) \* color.g + (0.114 + 0.886\*c - 0.203\*s) \* color.b\n );\n\}\n"

###### hue-shift.id

> `readonly` **id**: `"hue-shift"` = `'hue-shift'`

###### hue-shift.kind

> `readonly` **kind**: `"color"` = `'color'`

###### hue-shift.label

> `readonly` **label**: `"Hue Shift"` = `'Hue Shift'`

##### iridescent-sheen

> **iridescent-sheen**: `object` = `iridescentSheenBehavior`

###### iridescent-sheen.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### iridescent-sheen.glslFunction

> `readonly` **glslFunction**: "\nvec3 applyIridescence(vec3 baseColor, vec2 coords, float t) \{\n float factor = sin(coords.x \* 2.5 + t) \* cos(coords.y \* 2.5 - t);\n vec3 sheen = 0.5 + 0.5 \* cos(t + coords.xyx \* 3.0 + vec3(0.0, 2.0, 4.0));\n return mix(baseColor, baseColor + sheen \* 0.4, smoothstep(-0.5, 0.5, factor));\n\}\n"

###### iridescent-sheen.id

> `readonly` **id**: `"iridescent-sheen"` = `'iridescent-sheen'`

###### iridescent-sheen.kind

> `readonly` **kind**: `"color"` = `'color'`

###### iridescent-sheen.label

> `readonly` **label**: `"Iridescence"` = `'Iridescence'`

##### kaleidoscope

> **kaleidoscope**: `object` = `kaleidoscopeBehavior`

###### kaleidoscope.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### kaleidoscope.glslFunction

> `readonly` **glslFunction**: "vec2 kaleidoscope(vec2 coords, float t, float speed) \{\n float rot = t \* speed \* 0.1;\n float c = cos(rot);\n float s = sin(rot);\n coords = vec2(c \* coords.x - s \* coords.y, s \* coords.x + c \* coords.y);\n\n float r = length(coords);\n float a = atan(coords.y, coords.x);\n\n float slice = 3.14159265 / 6.0;\n a = mod(a + slice, 2.0 \* slice) - slice;\n a = abs(a);\n\n return r \* vec2(cos(a), sin(a));\n\}\n"

###### kaleidoscope.id

> `readonly` **id**: `"kaleidoscope"` = `'kaleidoscope'`

###### kaleidoscope.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### kaleidoscope.label

> `readonly` **label**: `"Kaleidoscope"` = `'Kaleidoscope'`

##### liquid-metal

> **liquid-metal**: `object` = `liquidMetalBehavior`

###### liquid-metal.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### liquid-metal.id

> `readonly` **id**: `"liquid-metal"` = `'liquid-metal'`

###### liquid-metal.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### liquid-metal.label

> `readonly` **label**: `"Liquid"` = `'Liquid'`

###### liquid-metal.noiseDependencies

> `readonly` **noiseDependencies**: readonly \[`"smoothNoise2"`\]

##### mirror-tile

> **mirror-tile**: `object` = `mirrorTileBehavior`

###### mirror-tile.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### mirror-tile.id

> `readonly` **id**: `"mirror-tile"` = `'mirror-tile'`

###### mirror-tile.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### mirror-tile.label

> `readonly` **label**: `"Mirror"` = `'Mirror'`

##### mouse-proximity

> **mouse-proximity**: `object` = `mouseProximityBehavior`

###### mouse-proximity.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### mouse-proximity.id

> `readonly` **id**: `"mouse-proximity"` = `'mouse-proximity'`

###### mouse-proximity.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### mouse-proximity.label

> `readonly` **label**: `"Mouse"` = `'Mouse'`

##### neon-glow-pulse

> **neon-glow-pulse**: `object` = `neonGlowPulseBehavior`

###### neon-glow-pulse.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### neon-glow-pulse.glslFunction

> `readonly` **glslFunction**: "vec3 neonGlow(vec3 color, float t, float speed) \{\n float lum = dot(color, vec3(0.299, 0.587, 0.114));\n float pulse = 0.5 + 0.5 \* sin(t \* speed \* 0.6);\n float glow = smoothstep(0.5, 0.9, lum) \* pulse;\n return color + glow \* vec3(0.3, 0.8, 1.0);\n\}\n"

###### neon-glow-pulse.id

> `readonly` **id**: `"neon-glow-pulse"` = `'neon-glow-pulse'`

###### neon-glow-pulse.kind

> `readonly` **kind**: `"color"` = `'color'`

###### neon-glow-pulse.label

> `readonly` **label**: `"Neon"` = `'Neon'`

##### neon-reactive

> **neon-reactive**: `object` = `neonReactivePulseBehavior`

###### neon-reactive.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### neon-reactive.id

> `readonly` **id**: `"neon-reactive"` = `'neon-reactive'`

###### neon-reactive.kind

> `readonly` **kind**: `"color"` = `'color'`

###### neon-reactive.label

> `readonly` **label**: `"Neon"` = `'Neon'`

##### noise-crawl

> **noise-crawl**: `object` = `noiseCrawlBehavior`

###### noise-crawl.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### noise-crawl.id

> `readonly` **id**: `"noise-crawl"` = `'noise-crawl'`

###### noise-crawl.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### noise-crawl.label

> `readonly` **label**: `"Crawl"` = `'Crawl'`

###### noise-crawl.noiseDependencies

> `readonly` **noiseDependencies**: readonly \[`"smoothNoise2"`\]

##### palette-cycle

> **palette-cycle**: `object` = `paletteCycleBehavior`

###### palette-cycle.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### palette-cycle.glslFunction

> `readonly` **glslFunction**: "vec3 iqPalette(float t) \{\n vec3 a = vec3(0.5, 0.5, 0.5);\n vec3 b = vec3(0.5, 0.5, 0.5);\n vec3 c = vec3(1.0, 1.0, 1.0);\n vec3 d = vec3(0.0, 0.33, 0.67);\n return a + b \* cos(6.28318 \* (c \* t + d));\n\}\n\nvec3 paletteCycleColor(vec3 baseColor, float t) \{\n float lum = dot(baseColor, vec3(0.299, 0.587, 0.114));\n return iqPalette(lum + t \* 0.15);\n\}\n"

###### palette-cycle.id

> `readonly` **id**: `"palette-cycle"` = `'palette-cycle'`

###### palette-cycle.kind

> `readonly` **kind**: `"color"` = `'color'`

###### palette-cycle.label

> `readonly` **label**: `"Cycle"` = `'Cycle'`

##### pixelation

> **pixelation**: `object` = `pixelationBehavior`

###### pixelation.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### pixelation.id

> `readonly` **id**: `"pixelation"` = `'pixelation'`

###### pixelation.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### pixelation.label

> `readonly` **label**: `"Pixel"` = `'Pixel'`

##### plasma-fluid

> **plasma-fluid**: `object` = `plasmaFluidBehavior`

###### plasma-fluid.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### plasma-fluid.id

> `readonly` **id**: `"plasma-fluid"` = `'plasma-fluid'`

###### plasma-fluid.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### plasma-fluid.label

> `readonly` **label**: `"Dynamics"` = `'Dynamics'`

###### plasma-fluid.noiseDependencies

> `readonly` **noiseDependencies**: readonly \[`"smoothNoise2"`\]

##### quantum-tessellation

> **quantum-tessellation**: `object` = `quantumTessellationBehavior`

###### quantum-tessellation.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### quantum-tessellation.glslFunction

> `readonly` **glslFunction**: "\nvec2 quantumGridHash(vec2 p) \{\n p = vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)));\n return fract(sin(p) \* 43758.5453123);\n\}\n\nvec2 quantumTessellation(vec2 p, float t, float speed) \{\n vec2 scaled = p \* 4.0;\n vec2 ip = floor(scaled);\n vec2 fp = fract(scaled);\n\n float minDist = 4.0;\n vec2 targetOffset = vec2(0.0);\n\n for (int j = -1; j \<= 1; j++) \{\n for (int i = -1; i \<= 1; i++) \{\n vec2 g = vec2(float(i), float(j));\n vec2 o = quantumGridHash(ip + g);\n o = 0.5 + 0.5 \* sin(t \* speed \* 0.5 + o \* 6.2831);\n vec2 r = g + o - fp;\n float d = dot(r, r);\n if (d \< minDist) \{\n minDist = d;\n targetOffset = o;\n \}\n \}\n \}\n float angle = t \* speed \* 0.15 + (targetOffset.x - targetOffset.y) \* 2.0;\n mat2 rot = mat2(cos(angle), -sin(angle), sin(angle), cos(angle));\n return mix(p, rot \* p, smoothstep(0.1, 0.9, minDist));\n\}\n"

###### quantum-tessellation.id

> `readonly` **id**: `"quantum-tessellation"` = `'quantum-tessellation'`

###### quantum-tessellation.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### quantum-tessellation.label

> `readonly` **label**: `"Quantum"` = `'Quantum'`

##### rgb-glitch-split

> **rgb-glitch-split**: `object` = `rgbGlitchSplitBehavior`

###### rgb-glitch-split.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### rgb-glitch-split.glslFunction

> `readonly` **glslFunction**: "vec3 rgbGlitchSplit(vec3 color, vec2 coords, float t, float speed) \{\n float band = floor(coords.y \* 20.0);\n float step_t = floor(t \* speed \* 3.0);\n float trigger = step(0.9, fract(sin(band \* 91.345 + step_t) \* 43758.5453));\n float rShift = trigger \* (fract(sin(band \* 12.9898 + step_t) \* 43758.5453) - 0.5) \* 0.4;\n color.r += rShift;\n color.b -= rShift;\n return color;\n\}\n"

###### rgb-glitch-split.id

> `readonly` **id**: `"rgb-glitch-split"` = `'rgb-glitch-split'`

###### rgb-glitch-split.kind

> `readonly` **kind**: `"color"` = `'color'`

###### rgb-glitch-split.label

> `readonly` **label**: `"RGB Split"` = `'RGB Split'`

##### rotate

> **rotate**: `object` = `rotateBehavior`

###### rotate.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### rotate.glslFunction

> `readonly` **glslFunction**: "mat2 rotate2d(float \_angle)\{\n return mat2(cos(\_angle),-sin(\_angle),\n sin(\_angle),cos(\_angle));\n\}\n"

###### rotate.id

> `readonly` **id**: `"rotate"` = `'rotate'`

###### rotate.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### rotate.label

> `readonly` **label**: `"Rotate"` = `'Rotate'`

##### scan-lines

> **scan-lines**: `object` = `scanLinesBehavior`

###### scan-lines.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### scan-lines.id

> `readonly` **id**: `"scan-lines"` = `'scan-lines'`

###### scan-lines.kind

> `readonly` **kind**: `"color"` = `'color'`

###### scan-lines.label

> `readonly` **label**: `"Scan"` = `'Scan'`

##### spectral-shift

> **spectral-shift**: `object` = `spectralShiftBehavior`

###### spectral-shift.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### spectral-shift.glslFunction

> `readonly` **glslFunction**: "\nvec3 spectralPalette(float t) \{\n vec3 a = vec3(0.5, 0.5, 0.5);\n vec3 b = vec3(0.5, 0.5, 0.5);\n vec3 c = vec3(2.0, 1.0, 0.0);\n vec3 d = vec3(0.5, 0.20, 0.25);\n return a + b \* cos(6.28318 \* (c \* t + d));\n\}\n"

###### spectral-shift.id

> `readonly` **id**: `"spectral-shift"` = `'spectral-shift'`

###### spectral-shift.kind

> `readonly` **kind**: `"color"` = `'color'`

###### spectral-shift.label

> `readonly` **label**: `"Palettizer"` = `'Palettizer'`

##### spiral-galaxy

> **spiral-galaxy**: `object` = `spiralGalaxyBehavior`

###### spiral-galaxy.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### spiral-galaxy.glslFunction

> `readonly` **glslFunction**: "vec2 spiralGalaxy(vec2 coords, float t, float speed) \{\n float r = length(coords);\n float a = atan(coords.y, coords.x);\n float spiralAmount = 3.0 \* log(r + 0.001) + t \* speed \* 0.3;\n a += spiralAmount \* 0.5;\n return vec2(cos(a), sin(a)) \* r;\n\}\n"

###### spiral-galaxy.id

> `readonly` **id**: `"spiral-galaxy"` = `'spiral-galaxy'`

###### spiral-galaxy.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### spiral-galaxy.label

> `readonly` **label**: `"Galaxy"` = `'Galaxy'`

##### swirl

> **swirl**: `object` = `swirlBehavior`

###### swirl.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### swirl.glslFunction

> `readonly` **glslFunction**: "vec2 swirl(vec2 coords, float angle) \{\n float r = length(coords);\n float a = atan(coords.y, coords.x) + angle \* (1.0 - r);\n return vec2(cos(a) \* r, sin(a) \* r);\n\}\n"

###### swirl.id

> `readonly` **id**: `"swirl"` = `'swirl'`

###### swirl.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### swirl.label

> `readonly` **label**: `"Swirl"` = `'Swirl'`

##### thermal-radiance

> **thermal-radiance**: `object` = `thermalRadianceBehavior`

###### thermal-radiance.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### thermal-radiance.glslFunction

> `readonly` **glslFunction**: "\nvec3 branchlessThermal(float t) \{\n vec3 c1 = vec3(0.0, 0.0, 0.15);\n vec3 c2 = vec3(0.7, 0.0, 0.55);\n vec3 c3 = vec3(1.0, 0.5, 0.0);\n vec3 c4 = vec3(0.98, 0.98, 0.8);\n\n float ramp1 = smoothstep(0.0, 0.33, t);\n float ramp2 = smoothstep(0.33, 0.66, t);\n float ramp3 = smoothstep(0.66, 1.0, t);\n\n vec3 col = mix(c1, c2, ramp1);\n col = mix(col, c3, ramp2);\n col = mix(col, c4, ramp3);\n return col;\n\}\n"

###### thermal-radiance.id

> `readonly` **id**: `"thermal-radiance"` = `'thermal-radiance'`

###### thermal-radiance.kind

> `readonly` **kind**: `"color"` = `'color'`

###### thermal-radiance.label

> `readonly` **label**: `"Radiance"` = `'Radiance'`

##### thermal-vision

> **thermal-vision**: `object` = `thermalVisionBehavior`

###### thermal-vision.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### thermal-vision.glslFunction

> `readonly` **glslFunction**: "vec3 thermalPalette(float t) \{\n vec3 c1 = vec3(0.0, 0.0, 0.2);\n vec3 c2 = vec3(0.8, 0.0, 0.6);\n vec3 c3 = vec3(1.0, 0.6, 0.0);\n vec3 c4 = vec3(1.0, 1.0, 0.8);\n if (t \< 0.33) return mix(c1, c2, t / 0.33);\n else if (t \< 0.66) return mix(c2, c3, (t - 0.33) / 0.33);\n else return mix(c3, c4, (t - 0.66) / 0.34);\n\}\n\nvec3 thermalVision(vec3 color, float t, float speed) \{\n float lum = dot(color, vec3(0.299, 0.587, 0.114));\n float shift = fract(lum + t \* speed \* 0.05);\n return mix(color, thermalPalette(shift), 0.7);\n\}\n"

###### thermal-vision.id

> `readonly` **id**: `"thermal-vision"` = `'thermal-vision'`

###### thermal-vision.kind

> `readonly` **kind**: `"color"` = `'color'`

###### thermal-vision.label

> `readonly` **label**: `"Thermal"` = `'Thermal'`

##### tunnel

> **tunnel**: `object` = `tunnelBehavior`

###### tunnel.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### tunnel.glslFunction

> `readonly` **glslFunction**: "vec2 tunnel(vec2 coords, float t, float speed) \{\n float r = length(coords) + 0.0001;\n float a = atan(coords.y, coords.x);\n float depth = fract(log(r) \* 0.5 - t \* speed \* 0.2);\n return vec2(a / 3.14159265, depth \* 2.0 - 1.0);\n\}\n"

###### tunnel.id

> `readonly` **id**: `"tunnel"` = `'tunnel'`

###### tunnel.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### tunnel.label

> `readonly` **label**: `"Tunnel"` = `'Tunnel'`

##### vignette

> **vignette**: `object` = `vignetteBehavior`

###### vignette.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### vignette.id

> `readonly` **id**: `"vignette"` = `'vignette'`

###### vignette.kind

> `readonly` **kind**: `"color"` = `'color'`

###### vignette.label

> `readonly` **label**: `"Vignette"` = `'Vignette'`

##### voronoi

> **voronoi**: `object` = `voronoiBehavior`

###### voronoi.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### voronoi.glslFunction

> `readonly` **glslFunction**: "vec2 voronoiHash(vec2 p) \{\n p = vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)));\n return -1.0 + 2.0 \* fract(sin(p) \* 43758.5453123);\n\}\n\nvec2 voronoiWarp(vec2 x, float t, float speed) \{\n vec2 n = floor(x);\n vec2 f = fract(x);\n float md = 8.0;\n vec2 mr = x;\n for (int j = -1; j \<= 1; j++) \{\n for (int i = -1; i \<= 1; i++) \{\n vec2 g = vec2(float(i), float(j));\n vec2 o = voronoiHash(n + g);\n o = 0.5 + 0.5 \* sin(t \* speed + 6.2831 \* o);\n vec2 r = g + o - f;\n float d = dot(r, r);\n if (d \< md) \{\n md = d;\n mr = r;\n \}\n \}\n \}\n return x + mr \* (0.5 + 0.5 \* sin(md \* 3.14159));\n\}\n"

###### voronoi.id

> `readonly` **id**: `"voronoi"` = `'voronoi'`

###### voronoi.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### voronoi.label

> `readonly` **label**: `"Voronoi"` = `'Voronoi'`

##### wave-interference

> **wave-interference**: `object` = `waveInterferenceBehavior`

###### wave-interference.applyCode

> `readonly` **applyCode**: (`__namedParameters`) => `string`

###### Parameters

###### \_\_namedParameters

[`ApplyCodeContext`](#applycodecontext)

###### Returns

`string`

###### wave-interference.glslFunction

> `readonly` **glslFunction**: "vec2 waveInterference(vec2 coords, float t, float speed) \{\n vec2 p1 = vec2(0.5, 0.3);\n vec2 p2 = vec2(-0.4, -0.5);\n vec2 p3 = vec2(-0.3, 0.6);\n float d1 = length(coords - p1);\n float d2 = length(coords - p2);\n float d3 = length(coords - p3);\n float wave = sin(d1 \* 10.0 - t \* speed)\n + sin(d2 \* 10.0 - t \* speed \* 1.3)\n + sin(d3 \* 10.0 - t \* speed \* 0.8);\n vec2 dir = normalize(coords + 0.0001);\n return coords + dir \* wave \* 0.03;\n\}\n"

###### wave-interference.id

> `readonly` **id**: `"wave-interference"` = `'wave-interference'`

###### wave-interference.kind

> `readonly` **kind**: `"spatial"` = `'spatial'`

###### wave-interference.label

> `readonly` **label**: `"Interference"` = `'Interference'`

## Functions

### getBehavior()

> **getBehavior**(`id`): [`Behavior`](#behavior)

Defined in: [behaviors/registry.ts:112](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L112)

Look up a behavior definition by id.

#### Parameters

##### id

`"hue-shift"` \| `"color-drift"` \| `"edge-detect"` \| `"vignette"` \| `"film-grain"` \| `"scan-lines"` \| `"spectral-shift"` \| `"iridescent-sheen"` \| `"thermal-radiance"` \| `"neon-reactive"` \| `"palette-cycle"` \| `"neon-glow-pulse"` \| `"rgb-glitch-split"` \| `"aurora-flow"` \| `"thermal-vision"` \| `"rotate"` \| `"swirl"` \| `"kaleidoscope"` \| `"domain-warp"` \| `"mirror-tile"` \| `"tunnel"` \| `"noise-crawl"` \| `"mouse-proximity"` \| `"pixelation"` \| `"voronoi"` \| `"spiral-galaxy"` \| `"gravity-lens"` \| `"wave-interference"` \| `"crystal-facet"` \| `"glitch-blocks"` \| `"liquid-metal"` \| `"cosmic-maelstrom"` \| `"quantum-tessellation"` \| `"gravity-well"` \| `"plasma-fluid"` \| `"cyber-chroma-glitch"`

#### Returns

[`Behavior`](#behavior)

---

### getBehaviorKinds()

> **getBehaviorKinds**(): [`BehaviorGroup`](#behaviorgroup)[]

Defined in: [behaviors/registry.ts:130](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine-next/src/behaviors/registry.ts#L130)

All behaviors grouped by [BehaviorKind](#behaviorkind-1), for building pickers.

#### Returns

[`BehaviorGroup`](#behaviorgroup)[]
