---
package: '@repo/randomart-engine'
kind: module
module: png-export
title: png-export
---

## Functions

### renderTreesToPngBlob()

> **renderTreesToPngBlob**(`treeR`, `treeG`, `treeB`, `size`, `time?`): `Blob`

Defined in: [png-export.ts:31](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine/src/png-export.ts#L31)

Renders the three channel trees to a `Blob` of MIME type `image/png`, ready for display or
download.

#### Parameters

##### treeR

[`ExpressionNode`](types.mdx#expressionnode)

##### treeG

[`ExpressionNode`](types.mdx#expressionnode)

##### treeB

[`ExpressionNode`](types.mdx#expressionnode)

##### size

`number`

##### time?

`number` = `0`

#### Returns

`Blob`

---

### renderTreesToPngBuffer()

> **renderTreesToPngBuffer**(`treeR`, `treeG`, `treeB`, `size`, `time?`): `Uint8Array`

Defined in: [png-export.ts:9](https://github.com/joska-p/playground/blob/b4f351259e36f80b1230d3ac42dacf689d4443d9/packages/randomart-engine/src/png-export.ts#L9)

Renders the three channel trees to an encoded PNG (`Uint8Array`), ready for disk or for wrapping
in a `Blob`.

#### Parameters

##### treeR

[`ExpressionNode`](types.mdx#expressionnode)

##### treeG

[`ExpressionNode`](types.mdx#expressionnode)

##### treeB

[`ExpressionNode`](types.mdx#expressionnode)

##### size

`number`

##### time?

`number` = `0`

#### Returns

`Uint8Array`
