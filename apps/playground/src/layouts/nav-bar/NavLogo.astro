---
import { toCanadianSyllabics } from '../../utils/format';

const logoTitle = toCanadianSyllabics.fromHash('PG_LAB');

type Props = {
    href: string;
};
const { href } = Astro.props;
---

<a
    aria-label="Playground home"
    class="group text-foreground pointer-events-auto flex items-center gap-2 text-xs font-bold transition-all duration-300"
    href={href}
>
    <span
        aria-hidden="true"
        class="relative flex h-6 w-6 items-center justify-center"
    >
        <span
            class="border-primary bg-primary absolute inset-0 animate-ping rounded-full border-2 opacity-30"
        ></span>
        <span
            class="bg-primary relative flex h-3 w-3 items-center justify-center rounded-full border-3 border-transparent shadow-[0_0_8px_1px_var(--primary)] transition-all duration-300 group-hover:scale-110 group-hover:shadow-[0_0_16px_3px_var(--primary)]"
        ></span>
    </span>
    {logoTitle}
</a>
