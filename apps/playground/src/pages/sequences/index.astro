---
import Layout from "../../layouts/layout.astro";
import { baseUrl } from "../../constants/routes";

const projects = [
  {
    label: "Sequence Renderer",
    href: `${baseUrl}/sequences/sequence-renderer/`,
    description: "Explore mathematical sequences through visualization",
  },
];
---

<Layout title="Sequences">
  <div class="mx-auto max-w-4xl px-4 py-12 sm:px-6 lg:px-8">
    <header class="mb-12">
      <h1 class="text-4xl font-bold text-foreground">Sequences</h1>
      <p class="mt-2 text-lg text-foreground/60">
        Math visualizations and sequence generators
      </p>
    </header>

    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {
        projects.map((project) => (
          <a
            href={project.href}
            class="group block rounded-lg border border-border bg-card p-6 transition-all hover:border-primary hover:bg-card/80"
          >
            <h2 class="text-xl font-semibold text-foreground group-hover:text-primary">
              {project.label}
            </h2>
            <p class="mt-2 text-sm text-foreground/60">{project.description}</p>
          </a>
        ))
      }
    </div>
  </div>
</Layout>