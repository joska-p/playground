---
import CategoryPage from "../../layouts/CategoryPage.astro";
import { baseUrl } from "../../constants/routes";

const title = "Sequences";
const description = "Math visualizations and sequence generators";
const projects = [
  {
    label: "Sequence Renderer",
    href: `${baseUrl}/sequences/sequence-renderer/`,
    description: "Explore mathematical sequences through visualization",
  },
];
---

<CategoryPage title={title} description={description} projects={projects} />