---
import CategoryPage from "../../layouts/category-page.astro";
import { baseUrl } from "../../constants/routes";

const title = "Colors";
const description = "Color tools and palette generators";
const projects = [
  {
    label: "Palettes Generator",
    href: `${baseUrl}/colors/palettes-generator/`,
    description: "Generate color palettes using algorithms",
  },
];
---

<CategoryPage title={title} description={description} projects={projects} />
