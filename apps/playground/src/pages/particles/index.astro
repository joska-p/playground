---
import CategoryLayout from "../../layouts/category/index.astro";
import { baseUrl } from "../../constants/routes";

const title = "Particles";
const description = "Visual effects using particle systems and physics";
const projects = [
  {
    label: "Image to Particles",
    href: `${baseUrl}/particles/image-to-particles/`,
    description: "Transform images into interactive falling particles",
  },
];
---

<CategoryLayout title={title} description={description} projects={projects} />
