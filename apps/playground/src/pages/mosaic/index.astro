---
import CategoryLayout from "../../layouts/category/category-layout.astro";
import { baseUrl } from "../../constants/routes";

const title = "Mosaic";
const description = "Pattern generation and tiling algorithms";
const projects = [
  {
    label: "Mosaic Maker",
    href: `${baseUrl}/mosaic/mosaic-maker/`,
    description: "Create complex mosaic patterns from images",
  },
];
---

<CategoryLayout title={title} description={description} projects={projects} />
