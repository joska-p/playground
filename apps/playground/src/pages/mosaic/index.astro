---
import CategoryPage from "../../layouts/CategoryPage.astro";
import { baseUrl } from "../../constants/routes";

const title = "Mosaic";
const description = "Pattern generation and tiling algorithms";
const projects = [
  {
    label: "Mosaic Maker",
    href: `${baseUrl}/mosaic/mosaic-maker/`,
    description: "Create complex mosaic patterns from images",
  },
];
---

<CategoryPage title={title} description={description} projects={projects} />